{
  "name": "lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "docker-build": "docker build -t lambda .",
    "docker-run": "docker run --name lambda -it lambda",
    "docker-cp": "docker cp lambda:/var/task/node_modules .",
    "zip": "zip -r nodejs"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.1657.0",
    "sharp": "^0.33.4"
  }
}
